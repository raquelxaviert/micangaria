<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="{{ settings.primary_color }}">
  
  <title>{{ shop.name }}</title>
  
  <meta name="description" content="{{ page_description | escape }}">
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
  
  <!-- Lucide Icons -->
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
  
  <!-- Theme CSS -->
  {{ 'theme.css' | asset_url | stylesheet_tag }}
  
  <style>
    :root {
      --primary: 25 53% 55%; /* #D4843E - terracota */
      --primary-foreground: 0 0% 100%;
      --secondary: 156 26% 33%; /* #3B6B4A - verde */
      --secondary-foreground: 0 0% 100%;
      --accent: 25 32% 24%; /* #4C3B27 - marrom escuro */
      --accent-foreground: 0 0% 100%;
      --background: 46 44% 94%; /* #F7F3ED - branco cru */
      --foreground: 25 32% 24%;
      --card: 0 0% 100%;
      --card-foreground: 25 32% 24%;
      --border: 25 20% 85%;
      --input: 25 20% 85%;
      --muted: 46 30% 90%;
      --muted-foreground: 25 15% 45%;
      --destructive: 0 70% 50%;
      --destructive-foreground: 0 0% 100%;
    }

    .password-page {
      min-height: 100vh;
      background: linear-gradient(135deg, hsl(var(--background)) 0%, hsl(var(--muted)) 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .password-container {
      background: hsl(var(--card));
      border-radius: 1rem;
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      max-width: 28rem;
      width: 100%;
      padding: 2rem;
      text-align: center;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      font-weight: 600;
      color: hsl(var(--primary));
      margin-bottom: 1rem;
    }

    .coming-soon {
      color: hsl(var(--muted-foreground));
      font-size: 0.875rem;
      margin-bottom: 2rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .description {
      color: hsl(var(--foreground));
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .password-form {
      margin-bottom: 2rem;
    }

    .form-group {
      position: relative;
      margin-bottom: 1rem;
    }

    .form-input {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 1px solid hsl(var(--border));
      border-radius: 0.5rem;
      background: hsl(var(--background));
      color: hsl(var(--foreground));
      font-size: 1rem;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .form-input:focus {
      outline: none;
      border-color: hsl(var(--primary));
      box-shadow: 0 0 0 3px hsl(var(--primary) / 0.1);
    }

    .btn-primary {
      width: 100%;
      padding: 0.75rem 1.5rem;
      background: hsl(var(--primary));
      color: hsl(var(--primary-foreground));
      border: none;
      border-radius: 0.5rem;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }

    .btn-primary:hover {
      background: hsl(var(--primary) / 0.9);
      transform: translateY(-1px);
    }

    .error-message {
      background: hsl(var(--destructive) / 0.1);
      color: hsl(var(--destructive));
      padding: 0.75rem;
      border-radius: 0.5rem;
      margin-bottom: 1rem;
      font-size: 0.875rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .social-links {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 2rem;
    }

    .social-link {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 2.5rem;
      height: 2.5rem;
      background: hsl(var(--muted));
      border-radius: 50%;
      color: hsl(var(--muted-foreground));
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .social-link:hover {
      background: hsl(var(--primary));
      color: hsl(var(--primary-foreground));
      transform: translateY(-2px);
    }

    .decorative-dots {
      position: absolute;
      top: 2rem;
      right: 2rem;
      width: 4rem;
      height: 4rem;
      opacity: 0.1;
    }

    .decorative-dots::before {
      content: '';
      position: absolute;
      width: 0.5rem;
      height: 0.5rem;
      background: hsl(var(--primary));
      border-radius: 50%;
      box-shadow: 
        1rem 0 hsl(var(--primary)),
        2rem 0 hsl(var(--primary)),
        0 1rem hsl(var(--primary)),
        1rem 1rem hsl(var(--primary)),
        2rem 1rem hsl(var(--primary)),
        0 2rem hsl(var(--primary)),
        1rem 2rem hsl(var(--primary)),
        2rem 2rem hsl(var(--primary));
    }
  </style>
</head>

<body class="password-page">
  <div class="decorative-dots"></div>
  
  <div class="password-container">
    <!-- Logo -->
    <h1 class="logo">miçangaria</h1>
    
    <!-- Coming Soon Badge -->
    <div class="coming-soon">Em breve</div>
    
    <!-- Description -->
    <p class="description">
      Estamos preparando algo especial para você. Nossa coleção de peças artesanais únicas 
      feitas com miçangas, cores vibrantes e muito amor estará disponível em breve.
    </p>

    <!-- Password Form -->
    {% if form.errors %}
      <div class="error-message">
        <i data-lucide="alert-circle" style="width: 1rem; height: 1rem;"></i>
        <span>{{ form.errors.messages['password'] | default: 'Senha incorreta. Tente novamente.' }}</span>
      </div>
    {% endif %}

    {% form 'storefront_password' %}
      <div class="password-form">
        <div class="form-group">
          <input 
            type="password" 
            name="password" 
            class="form-input"
            placeholder="Digite a senha de acesso"
            autofocus
          >
        </div>
        
        <button type="submit" class="btn-primary">
          <i data-lucide="unlock" style="width: 1rem; height: 1rem;"></i>
          Entrar
        </button>
      </div>
    {% endform %}

    <!-- Newsletter Signup -->
    <div style="border-top: 1px solid hsl(var(--border)); padding-top: 2rem;">
      <h3 style="margin-bottom: 1rem; color: hsl(var(--foreground)); font-size: 1.125rem;">
        Seja o primeiro a saber
      </h3>
      <p style="margin-bottom: 1rem; color: hsl(var(--muted-foreground)); font-size: 0.875rem;">
        Cadastre-se para receber uma notificação quando a loja estiver disponível
      </p>
      
      {% form 'customer' %}
        <input type="hidden" name="contact[tags]" value="newsletter,early-access">
        <div style="display: flex; gap: 0.5rem;">
          <input 
            type="email" 
            name="contact[email]" 
            class="form-input"
            placeholder="Seu e-mail"
            style="flex: 1;"
            required
          >
          <button type="submit" class="btn-primary" style="width: auto; white-space: nowrap; padding: 0.75rem 1rem;">
            <i data-lucide="send" style="width: 1rem; height: 1rem;"></i>
          </button>
        </div>
      {% endform %}
    </div>

    <!-- Social Links -->
    <div class="social-links">
      <a href="#" class="social-link" title="Instagram">
        <i data-lucide="instagram" style="width: 1.25rem; height: 1.25rem;"></i>
      </a>
      <a href="#" class="social-link" title="Facebook">
        <i data-lucide="facebook" style="width: 1.25rem; height: 1.25rem;"></i>
      </a>
      <a href="mailto:contato@micangaria.com.br" class="social-link" title="E-mail">
        <i data-lucide="mail" style="width: 1.25rem; height: 1.25rem;"></i>
      </a>
    </div>
  </div>

  <!-- Initialize Lucide Icons -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      lucide.createIcons();
    });
  </script>
</body>
</html>
