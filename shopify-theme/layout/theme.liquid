<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <title>{{ shop.name }}{% if page_title != blank %} - {{ page_title }}{% endif %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="{{ page_description | default: shop.description }}">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- CSS -->
    <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
    
    <!-- Lucide icons via CDN -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    
    {{ content_for_header }}
    
    <style>
      :root {
        --background: 40 33% 96%; /* Branco cru */
        --foreground: 27 48% 20%; /* Marrom escuro */
        --card: 0 0% 100%; /* Branco puro para contraste */
        --card-foreground: 27 48% 20%; /* Marrom escuro */
        --primary: 24 56% 51%; /* Terracota queimado */
        --primary-foreground: 40 33% 96%; /* Branco cru para texto no terracota */
        --secondary: 146 36% 35%; /* Verde folha */
        --secondary-foreground: 40 33% 96%; /* Branco cru para texto no verde */
        --muted: 46 77% 85%; /* Amarelo palha */
        --muted-foreground: 27 48% 40%; /* Marrom médio */
        --accent: 146 36% 45%; /* Verde folha mais claro */
        --accent-foreground: 27 48% 20%;
        --destructive: 0 75% 60%;
        --destructive-foreground: 0 0% 98%;
        --border: 40 20% 85%; /* Bege claro para bordas */
        --input: 0 0% 100%;
        --ring: 24 56% 60%; /* Terracota mais claro para foco */
      }
      
      body {
        background: hsl(var(--background));
        color: hsl(var(--foreground));
      }
      
      .btn-primary {
        background: hsl(var(--primary));
        color: hsl(var(--primary-foreground));
      }
      
      .btn-primary:hover {
        background: hsl(var(--primary) / 0.9);
      }
      
      .btn-outline {
        color: hsl(var(--primary));
        border-color: hsl(var(--primary));
      }
      
      .btn-outline:hover {
        background: hsl(var(--primary));
        color: hsl(var(--primary-foreground));
      }
      
      .card {
        background: hsl(var(--card));
        color: hsl(var(--card-foreground));
      }
      
      .text-primary { color: hsl(var(--primary)); }
      .text-secondary { color: hsl(var(--secondary)); }
      .text-muted { color: hsl(var(--muted-foreground)); }
      
      .bg-primary { background: hsl(var(--primary)); }
      .bg-secondary { background: hsl(var(--secondary)); }
      .bg-accent { background: hsl(var(--accent)); }
      .bg-card { background: hsl(var(--card)); }
      
      .border-primary { border-color: hsl(var(--primary) / 0.3); }
      
      .badge-primary {
        background: hsl(var(--primary) / 0.1);
        color: hsl(var(--primary));
        border: 1px solid hsl(var(--primary) / 0.3);
      }
      
      .badge-secondary {
        background: hsl(var(--secondary) / 0.1);
        color: hsl(var(--secondary));
        border: 1px solid hsl(var(--secondary) / 0.3);
      }
      
      .badge-accent {
        background: hsl(var(--accent));
        color: hsl(var(--accent-foreground));
      }
      
      .badge-destructive {
        background: hsl(var(--destructive));
        color: hsl(var(--destructive-foreground));
      }
    </style>
  </head>
  <body class="flex flex-col min-h-screen">
    {% section 'header' %}
    <main class="main-content">
      {{ content_for_layout }}
    </main>
    {% section 'footer' %}
    
    <script>
      // Initialize Lucide icons
      lucide.createIcons();
    </script>
  </body>
</html>
